# ── Email delivery for website messages ────────────────────────────────
# The Contact form, Careers applications, and lead capture all send you an
# email via Gmail/Google Workspace SMTP (nodemailer).
#
# GMAIL_USER          The mailbox that SENDS the notifications (e.g. your
#                     Google Workspace info@hitroo.com, or any Gmail).
# GMAIL_APP_PASSWORD  A Google "App Password" — NOT your normal password.
#                     Create at: Google Account → Security → 2-Step
#                     Verification → App passwords.
GMAIL_USER=info@hitroo.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# Where website messages are delivered. Defaults to info@hitroo.com if unset.
LEAD_EMAIL_RECIPIENT=info@hitroo.com

# ── Optional but recommended: contact-form bot verification ────────────
# Create a Cloudflare Turnstile widget restricted to the production
# hostname(s), then set BOTH values. If either is missing, Turnstile stays
# disabled and the server continues to enforce validation, honeypot/timing
# checks, payload limits, and Netlify rate limiting.
#
# The site key is public; the secret key must only be set in server/Netlify env.
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ── Admin content manager (/admin) ─────────────────────────────────────
ADMIN_PASSWORD=change-me

# ── Optional: legacy AI chat route (not used by the current UI) ─────────
GROQ_API_KEY=

# ── Database (shared Postgres on Fly.io, app `hitroo-db`) ─────────────────────
# The site connects as the least-privilege `web_app` role (TLS, verify-full):
#   postgres://web_app:<password>@hitroo-db.fly.dev:5432/hitroo?sslmode=verify-full
DATABASE_URL=
# Local only — for `npm run db:migrate` (postgres role on the hitroo database).
# Never set this on Vercel.
DATABASE_ADMIN_URL=

# ── Site / SEO ──────────────────────────────────────────────────────────────
# Canonical origin (defaults to https://www.hitroo.com, the primary domain). Set it for preview deployments.
NEXT_PUBLIC_SITE_URL=
# Search Console / Bing Webmaster Tools verification tokens (optional).
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=
